# Agentic AI - 하네스 엔지니어링

## 언어 사용 규칙

- 모든 응답과 설명은 한국어로 작성한다.
- 코드 내 주석·로그 출력·오류 메시지는 한국어로 작성한다.
- 변수명·함수명·클래스명은 영어(snake_case / PascalCase)로 작성한다.
- 기술 용어(API, LLM, token 등)는 영어 원어 그대로 사용한다.

---

## 개발 규칙

- 새 도구(tool)는 `tools/` 아래 모듈로 추가한다.
- 모든 도구 함수는 `dict`를 반환해 에이전트가 결과를 파싱하기 쉽게 한다.
- 민감 정보(API 키, 경로)는 반드시 `.env`에서 읽고, 코드에 하드코딩하지 않는다.
- 파괴적 작업(파일 삭제, 프로세스 종료)은 사용자 확인 후 실행한다.
- 에러는 함수 밖으로 전파하지 않고 내부에서 처리한다.
- 작업 전 해당 폴더의 `SKILL.md`를 반드시 먼저 읽는다.

### 새 모듈 추가 절차 (순서 엄수)

1. `features/` 안에 새 폴더 생성
2. `interface.py` 먼저 작성 (입력·반환 타입 정의)
3. `core/`에 호출 추가 (`core/`만 건드림)
4. `internal/` 구현
5. `SKILL.md` 작성

> `interface.py`·`core/`는 보호 컴포넌트. 수정 시 반드시 사용자 승인 후 진행.
> `interface.py` 생성 시 상단에 버전 태그 필수: `# interface version: 1.0`

```python
def some_tool(...) -> dict:
    try:
        return {"결과": "성공", ...}
    except Exception as e:
        return {"결과": "실패", "이유": str(e)}
```

---

## 보호 컴포넌트 수정 절차

핵심 컴포넌트를 수정하기 전 아래 절차를 반드시 따른다. 사용자 승인 없이 임의로 수정하지 않는다.

1. **작업 중단** — 보호 컴포넌트 수정이 필요한 시점에서 즉시 멈춘다.
2. **이유 보고** — 아래 형식으로 사용자에게 설명한다.

   ```
   [보호 컴포넌트 수정 요청]
   - 수정 대상 파일: <파일 경로>
   - 수정이 필요한 이유: <왜 수정하지 않으면 구현이 불가능한지>
   - 변경 범위: <어떤 부분을 어떻게 바꿀 것인지>
   - 발생 가능한 부작용: <영향을 받을 수 있는 다른 코드>
   승인하시면 진행하겠습니다.
   ```

3. **승인 대기** — 사용자가 명시적으로 승인한 경우에만 수정을 진행한다.
4. **수정 후 보고** — 변경된 내용을 간략히 요약한다.

---

## requirements.txt 관리 규칙

- **삭제 금지**: 한 번 기록된 라이브러리는 코드에서 더 이상 사용하지 않더라도 삭제하지 않는다. PC 교체 시 동일 환경 복원을 위한 영구 기록부다.
- **신규 추가 의무**: 새 라이브러리 설치 시 반드시 `requirements.txt`에 추가한다.
- **버전 업그레이드 시**: 기존 버전을 주석으로 보존한 채 새 버전으로 교체한다.

```
# anthropic>=0.40.0  (구버전 — 롤백 필요 시 주석 해제)
anthropic>=0.50.0
```

---

## 자동 Git Push 규칙

### Push 트리거 조건

| 조건 | 설명 |
|------|------|
| **개발 중단 명령** | "개발 그만", "중단", "여기까지", "멈춰" 등 종료 의사 표현 시 |
| **일일 토큰 소진 임박** | 하루 토큰 사용량 95% 소진 시 |
| **자정 임박** | 오후 11시 30분 — 작업 마무리 후 push |

### Push 절차

```
1. 당일 작업 브리핑을 사용자에게 먼저 전달한다
2. git status 로 변경 파일 확인
3. .env 파일은 절대 staging 하지 않는다
4. README.md 를 현재 구현 상태에 맞게 최신화한다
5. 변경된 파일을 파일명 지정 방식으로 git add
6. 커밋 메시지 작성 (한국어, 변경 내용 요약)
7. git push
8. Push 완료 여부를 사용자에게 보고한다
```

**당일 작업 브리핑 형식**

```
📋 오늘 작업 브리핑
─────────────────────────────
✅ 완료한 작업
  • [파일명] 무엇을 왜 만들었는지 한 줄 설명

🔧 수정한 내용
  • [파일명] 어떤 부분을 왜 바꿨는지 한 줄 설명

📌 현재 상태
  • 지금 프로젝트가 어디까지 완성됐는지 한 문장 요약

⏭ 다음에 할 것 (있는 경우)
  • 다음 세션에서 이어서 해야 할 작업
─────────────────────────────
```

---

## 보안 규칙

- `subprocess`, `os.system`, `eval`, `exec` 사용 금지 (임의 명령 실행 방지)
- 아래 시스템 경로 접근 금지:
  - Windows: `C:\Windows`, `C:\Program Files`, `C:\Program Files (x86)`
  - 공통: `/etc`, `/sys`, `/proc`, `/boot`
- `.env`는 절대 `git add` 하지 않는다.
